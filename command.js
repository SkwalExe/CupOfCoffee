module.exports = {
          name: String,
          description: String,
          guildOnly: Boolean,
          use: String,
          type: String,
          aliases: [],

          execute(message, args) {
                    const Discord = require('discord.js');
                    const bot = require('../bot');
          }
}